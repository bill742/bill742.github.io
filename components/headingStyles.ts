import { background, color } from '@chakra-ui/react';

export const HeadingStyles = {
  baseStyle: () => ({
    marginBottom: '3rem',
  }),
  defaultProps: {},
  sizes: {},
  variants: {
    header: () => ({
      textTransform: 'uppercase',
      marginBottom: 0,
    }),
  },
};
