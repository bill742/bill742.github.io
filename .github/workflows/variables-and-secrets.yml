name: Secrets and Environment Variables

on:
  workflow_dispatch:

jobs:
  top-secret:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_ALLOW_PORTFOLIO: ${{ vars.NEXT_PUBLIC_ALLOW_PORTFOLIO }}
      NEXT_PUBLIC_SITE_TITLE: ${{ env.NEXT_PUBLIC_SITE_TITLE}}

    steps:
      - name: Read a secret
        run: echo "The secret is ${{ secrets.NEXT_TEST_VALUE }}" && echo "The secret is ${{ secrets.NEXT_PUBLIC_LINKEDIN_USERNAME }}"

      - name: Read environment variables
        run: echo "Public site url - ${{ env.NEXT_PUBLIC_SITE_URL }}"
        #     echo "Allow Portfiolio section - ${{ env.NEXT_PUBLIC_ALLOW_PORTFOLIO }}"
        #     echo "Site title - ${{ env.NEXT_PUBLIC_SITE_TITLE }}"
        #     echo "Public email address - ${{ env.NEXT_PUBLIC_EMAIL_ADDRESS }}"
        #     echo "GitHub username - ${{ env.NEXT_PUBLIC_GH_USERNAME }}"
        #     echo "LinkedIn username - ${{ env.NEXT_PUBLIC_LINKEDIN_USERNAME }}"
